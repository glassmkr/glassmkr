# Capture manifest for homepage and marketing evidence exhibits.
# Required by the redesign specification, section 7.4. Every entry must name a
# real artifact, and scripts/check-exhibits.mjs recomputes the hash, so an
# exhibit cannot drift from the data it claims to show.
#
# Two exhibit forms are allowed: a browser or terminal capture, or a
# recorded-data render (the production component rendered from immutable
# captured data). This entry is the second form, which is why viewport is
# "responsive" rather than a pixel size: there is no image, so the exhibit
# keeps real HTML, real text selection, and real accessibility, and it reflows
# on a phone instead of becoming an unreadable screenshot.

exhibits:
  - id: demo-fleet-raid
    captured_at: 2026-08-30T10:31:00Z
    host_alias: db-hel1-01
    scenario_id: demo-sample-fleet
    crucible_version: 0.13.6
    dashboard_commit: "13cad34"
    source_environment: public demo
    viewport: responsive
    artifact_path: apps/site/src/lib/data/exhibits/demo-fleet-raid.json
    source_data_path: apps/site/src/lib/data/exhibits/demo-fleet-raid.json
    sha256: 6dc924616825c37bb28c8c6f57782b5f708d70d35ba4f880ad946869d41138c0
    notes: >
      The read-only sample fleet the public demo serves at
      app.glassmkr.com/demo, captured verbatim from the production dashboard's
      own API on 2026-08-30: the four demo hosts with their alert counts, and
      the db-hel1-01 raid_degraded alert exactly as the product renders it
      (the dashboard shows it as P0 CRITICAL; severity=critical bumps the
      rule's worst-case P1 per the canonical priority model). This is recorded
      demonstration data and every surface that renders it says so; the demo
      itself carries the same "read-only capture, not a live system" ribbon.
      The collector_version (0.13.6) is the version recorded in the sample
      capture, which predates the current release; the provenance line states
      the source rather than pretending the sample is a live fleet.

      The homepage product stage renders this exhibit. It replaced a
      hand-written fleet table (storage-fra-2, web-ams-1, db-ams-1) whose
      hostnames and telemetry were invented, which the redesign spec's truth
      test forbids.

  - id: val-debian-os-eol
    captured_at: 2026-08-27T06:42:52Z
    host_alias: val-debian
    scenario_id: unarmed-baseline
    crucible_version: 1.0.1
    dashboard_commit: 00a2ee67f9513a9a27a872f795cd146f49a8cdd7
    source_environment: validation fleet
    viewport: responsive
    artifact_path: apps/site/src/lib/data/exhibits/val-debian-os-eol.json
    source_data_path: apps/site/src/lib/data/exhibits/val-debian-os-eol.json
    sha256: ac1cc4c37a7d0f35fbd95bce818ca535f7a8975c43770e3879cd011c5ff39dc6
    notes: >
      A real alert from a real machine, not an armed scenario: a Supermicro
      SYS-530MT-H8TNR running Debian 12, reporting through Crucible 1.0.1 to a
      self-hosted dashboard on the same box. The alert body, recommendation and
      evidence are copied verbatim from the API response; the priority,
      category and quick-check command come from the shipped rule definition.
      The exhibit renders no IP address, no serial, and no key, because the
      alert card does not contain them. This is a deliberate boundary, not
      redaction: nothing was painted over.

      RECAPTURED 2026-08-27, first at Crucible 1.0.0 and again at 1.0.1 when that
      release shipped hours later, so the homepage does not show a superseded
      agent at launch. The host's agent was upgraded and the alert was
      re-read from the API; exactly two values moved, last_seen and
      crucible_version. Every other field is byte-identical to the 0.15.1
      capture, which is itself evidence that nothing was reshaped to look
      better. The dashboard was NOT rebuilt (its container predates both
      captures), so dashboard_commit is unchanged and still honest.

      GAP AGAINST SPEC 14.1, ACCEPTED BY SIMON 2026-08-26: the specification
      asks the first homepage exhibit to show a HARDWARE or NETWORK alert. This
      one is an OS-lifecycle alert, because this host's hardware is genuinely
      healthy and there is nothing wrong to show.

      Decision: ship this exhibit and keep the gap recorded honestly. Do NOT
      arm an md fault on val-debian, or on any box that has another role. When
      a real hardware alert appears anywhere in the fleet after launch, swap
      the exhibit through this manifest: add the new capture, verify the hash,
      repoint the component. Any future ARMED hardware capture happens on a
      scratch box with no other purpose.

      Never substitute a synthetic hardware alert to close this gap.
  - id: val-rocky-ssh-root
    captured_at: 2026-08-30T18:31:39Z
    host_alias: val-rocky
    scenario_id: unarmed-baseline
    crucible_version: 1.1.1
    dashboard_commit: "38d53e5"
    source_environment: validation fleet
    viewport: responsive
    artifact_path: apps/site/src/lib/data/exhibits/val-rocky-ssh-root.json
    source_data_path: apps/site/src/lib/data/exhibits/val-rocky-ssh-root.json
    sha256: 1282f61a604c5a30902628c852b39902aaee1aa437d91a55dee6bca45f1d87e8
    notes: >
      A real, unarmed alert from the validation fleet, read from the account
      API on 2026-08-30: val-rocky shipped from the provider with password
      root SSH enabled, which is exactly the class of quiet risk the
      /for-compute audience page exists to show. Alert body, evidence, and
      recommendation are verbatim. Renders as the first proof on
      /for-compute.
  - id: gpu-l4-baseline
    captured_at: 2026-08-30T18:27:41Z
    host_alias: glassmkr-gpu-1
    scenario_id: unarmed-baseline
    crucible_version: 1.1.1
    dashboard_commit: "38d53e5"
    source_environment: core production fleet
    viewport: responsive
    artifact_path: apps/site/src/lib/data/exhibits/gpu-l4-baseline.json
    source_data_path: apps/site/src/lib/data/exhibits/gpu-l4-baseline.json
    sha256: c9f5ef773df451e8a96b0168258fb5a053174055599dcb68dc91b9f414959bec
    notes: >
      The GPU telemetry surface of glassmkr-gpu-1 (the L4 that serves
      Glassmkr's own AI inference), read from the account API on 2026-08-30,
      plus the host's one real active alert (kernel_needs_reboot). The fleet
      currently has NO GPU fault, and the truth test forbids inventing one:
      the /for-gpu first proof is therefore an honestly healthy GPU baseline
      (ECC on, zero uncorrected, no throttle) with a real actionable alert on
      the same host. Swap in a real XID/ECC capture through this manifest if
      one ever occurs.


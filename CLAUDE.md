# CLAUDE.md

See [AGENTS.md](AGENTS.md). It is the canonical instruction file for this
repository and this file deliberately adds nothing to it.

Keeping two files in step is a job nobody does, and the failure mode is a second
copy that quietly stops matching the first. If something belongs in an agent's
instructions, it belongs in `AGENTS.md`.
